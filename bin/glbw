#!/bin/bash

# Deletes all local branches (except main). Stands for: Git Local Branch Wipe

main_branch="${1:-main}"

set -x

git branch |
	grep --invert-match "$main_branch" |
	xargs git branch --delete --force
