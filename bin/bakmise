#!/bin/bash

# Backs up mise config into dots

cp ~/.config/mise/config.toml "$DOTS/config-files/mise.config.toml"

DATE=$(date)

cd "$DOTS" && git add . && git commit -m "Back up mise config: $DATE" && git push
