#!/bin/bash

# glpr = Git Prune Local Repo = Prunes all branches that have been merged into the master branch.

git remote prune origin

git branch --merged |
	grep --invert-match --extended-regexp "(^\*|master|main)" |
	xargs git branch --delete
